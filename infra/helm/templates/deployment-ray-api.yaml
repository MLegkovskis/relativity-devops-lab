apiVersion: apps/v1
kind: Deployment
metadata:
  name: ray-api
  labels:
    app: ray-api
    app.kubernetes.io/name: ray-api
    app.kubernetes.io/part-of: blackhole
    tier: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ray-api
  template:
    metadata:
      labels:
        app: ray-api
        app.kubernetes.io/name: ray-api
        app.kubernetes.io/part-of: blackhole
        tier: api
    spec:
      containers:
        - name: ray-api
          image: "{{ .Values.repository }}:ray-api-dev"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.rayApi.port | default 8000 }}
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: {{ .Values.rayApi.port | default 8000 }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: {{ .Values.rayApi.port | default 8000 }}
            initialDelaySeconds: 10
            periodSeconds: 20
